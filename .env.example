OPENAI_API_KEY=

# The background canvas worker defaults to OpenAI gpt-5-mini and reuses the
# key above. To use OpenRouter instead, set all three values explicitly.
# CANVAS_AGENT_PROVIDER=openrouter
# CANVAS_AGENT_MODEL=openai/gpt-5-mini
# OPENROUTER_API_KEY=
